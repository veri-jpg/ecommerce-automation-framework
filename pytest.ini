[pytest]
testpaths = tests
addopts = -ra --reruns 1 --reruns-delay 2
markers =
    smoke: critical happy-path scenarios (login, checkout) - fast subset run in CI
    regression: broader coverage, run outside the critical path
